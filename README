# Excel Modifier Tool

## Description

The **Excel Modifier Tool** is a CLI (Command Line Interface) tool developed in Node.js to dynamically modify Excel spreadsheets (.xlsx). It allows users to apply arithmetic operations (+, -, *, /) to specific cells in multiple sheets within an Excel file and save the result in a new Excel file.

## Features

- Read existing Excel files (.xlsx).
- Apply arithmetic operations to specific cells in spreadsheets.
- Support for multiple sheets within the same file.
- Remove protection and unlock all cells to allow further editing.
- Save the modified file as a new Excel file.

## Prerequisites

- Node.js (version 12 or higher)
- NPM (Node Package Manager)

## Installation

1. Clone this repository:

   ```bash
   git clone https://github.com/your-username/excel-modifier-tool.git
